function handleClick(element) {

    /* handle buy element */
    if (element == 'buy') {
        console.log('buy clicked');
    } 

    /* handle other requests */
    else {
        console.log(element);
    }

}